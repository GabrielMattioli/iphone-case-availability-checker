# Example configuration file
# DO NOT use real credentials here. This is just a template.
# Copy this file to 'config.py' and replace with your actual information.

# Email configuration
SENDER_EMAIL = 'your_email@example.com'
EMAIL_PASSWORD = 'your_email_password'
RECIPIENT_EMAIL = 'recipient_email@example.com'

# Note: It's recommended to use app-specific passwords or OAuth2 for increased security
# when authenticating with email services.

# IMPORTANT: Keep your actual 'config.py' file private and never commit it to version control.
